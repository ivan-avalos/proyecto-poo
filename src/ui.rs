/*extern crate azul;

use azul::{prelude::*, widgets::{label::Label, button::Button}};

pub struct MainMenu {}

impl Layout for MainMenu {
    fn layout(&self, _info: LayoutInfo<Self>) -> Dom<Self> {
        let button = Button::with_label("Aña")
    }
}*/